spring:
  datasource:

    url: jdbc:mysql://onehana-db.casgypepjzlo.ap-northeast-2.rds.amazonaws.com:3306/OneHanaCICDServer
    username: admin
    password: ONEHANA0401!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
    database: mysql
    properties:
      hibernate:
        format_sql: true
        #        show_sql: true
        #        use_sql_comments: true
        default_batch_fetch_size: 100
    sql.init.mode: always

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace